name: Context examples

on: ['push']

jobs: 
  my-context-job:
    runs-on: ubuntu-latest
    steps: 
      - name: "my step"
        run: echo "Hello ${{ env.MY_ENV }}"
        env: 
          MY_ENV: "variabila mea si numai a mea" 